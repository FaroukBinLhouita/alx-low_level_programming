#include "main.h"

/**
 * _isupper - get the upper case
 * @c: the varabile who the fuction check
 * Return: 1 if the varabile is an uppercase otherwise 0
 */
int _isupper(int c)
{
return (c >= 'A' && c <= 'z');
}
